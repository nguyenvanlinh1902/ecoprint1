rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule - change this based on your security requirements
    match /{document=**} {
      allow read, write: if false;
    }
  }
} 